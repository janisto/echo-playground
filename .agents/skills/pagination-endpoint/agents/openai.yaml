interface:
  display_name: "Pagination Endpoint"
  short_description: "Build cursor-paginated Echo list endpoints"
  default_prompt: "Use $pagination-endpoint to implement and test a cursor-paginated Echo list endpoint."
