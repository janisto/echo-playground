interface:
  display_name: "Echo Endpoint"
  short_description: "Build project-standard Echo v5 API endpoints"
  default_prompt: "Use $echo-endpoint to implement an Echo v5 endpoint with the repository routing, validation, response, OpenAPI, and test conventions."
