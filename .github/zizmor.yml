rules:
  dependabot-cooldown:
    config:
      # Match zizmor's recommended minimum release cooldown.
      days: 7
  unpinned-uses:
    config:
      # Repository policy requires explicit full release tags.
      policies:
        "*": ref-pin
